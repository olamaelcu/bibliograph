{
  "lexicon": 1,
  "id": "community.lexicon.book.contributor.get",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "did", "record"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri" },
            "did": { "type": "string", "format": "did" },
            "record": { "type": "ref", "ref": "community.lexicon.book.contributor" },
            "cid": { "type": "string", "format": "cid" }
          }
        }
      },
      "errors": [
        { "name": "NotFound" }
      ]
    }
  }
}
