{
  "lexicon": 1,
  "id": "community.lexicon.book.status.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "bookUri": { "type": "string", "format": "at-uri" },
          "status": {
            "type": "string",
            "enum": ["reading", "read", "to-read", "abandoned"]
          },
          "cursor": { "type": "string" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["statuses"],
          "properties": {
            "statuses": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["uri", "did", "bookUri", "record"],
                "properties": {
                  "uri": { "type": "string", "format": "at-uri" },
                  "did": { "type": "string", "format": "did" },
                  "bookUri": { "type": "string", "format": "at-uri" },
                  "record": { "type": "object" }
                }
              }
            },
            "cursor": { "type": "string" }
          }
        }
      }
    }
  }
}
