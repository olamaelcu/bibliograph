{
  "lexicon": 1,
  "id": "community.lexicon.book.searchBooks",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["q"],
        "properties": {
          "q": { "type": "string" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 20 },
          "cursor": { "type": "string" }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["books"],
          "properties": {
            "books": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["uri", "record"],
                "properties": {
                  "uri": { "type": "string", "format": "at-uri" },
                  "record": { "type": "object" }
                }
              }
            },
            "cursor": { "type": "string" },
            "total": { "type": "integer" }
          }
        }
      }
    }
  }
}
