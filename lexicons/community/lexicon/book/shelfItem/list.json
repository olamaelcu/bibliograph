{
  "lexicon": 1,
  "id": "community.lexicon.book.shelfItem.list",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["shelfUri"],
        "properties": {
          "shelfUri": { "type": "string", "format": "at-uri" },
          "cursor": { "type": "string" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["items"],
          "properties": {
            "items": {
              "type": "array",
              "items": {
                "type": "object",
                "required": ["uri", "did", "record"],
                "properties": {
                  "uri": { "type": "string", "format": "at-uri" },
                  "did": { "type": "string", "format": "did" },
                  "record": { "type": "ref", "ref": "community.lexicon.book.shelfItem" }
                }
              }
            },
            "cursor": { "type": "string" }
          }
        }
      }
    }
  }
}
