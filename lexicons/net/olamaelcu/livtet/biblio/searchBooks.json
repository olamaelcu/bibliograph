{
  "lexicon": 1,
  "id": "net.olamaelcu.livtet.biblio.searchBooks",
  "description": "Search book records by title, description, or external identifier.",
  "defs": {
    "main": {
      "type": "query",
      "parameters": {
        "type": "params",
        "required": ["q"],
        "properties": {
          "q": {
            "type": "string",
            "description": "Search query string. Matches against title, description, and external identifiers."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 25,
            "description": "Maximum number of books to return."
          },
          "cursor": {
            "type": "string",
            "description": "Opaque pagination cursor."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["books"],
          "properties": {
            "books": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "net.olamaelcu.livtet.biblio.defs#bookView"
              }
            },
            "hitsTotal": {
              "type": "integer",
              "description": "Total number of matching books. May be rounded or truncated."
            },
            "cursor": {
              "type": "string"
            }
          }
        }
      }
    }
  }
}
